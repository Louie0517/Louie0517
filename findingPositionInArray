#include <stdio.h>

int funct();

int main(){
  
  funct();
  
  return 0;
  }
  
  int funct(){
    
    int N[3], D, size;
  size = sizeof(N) / sizeof(N[0]);
  printf("Input: ");
  for(int i = 0; i < size; i++){
  scanf("%i", &N[i]);
  }
  scanf("%i", &D);
  for(int i = 0; i < size; i++){
  if(N[i] == D){
  printf("Position: %i", i);
  return 0;
    } 
   }
     printf("Element not found");
     return -1;
 }